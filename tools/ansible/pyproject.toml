[project]
name = "ansible-tools"
version = "0.1.0"
description = "Ansible and ansible-lint for CI and local use"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "ansible>=13.3.0",
    "ansible-lint>=26.1.1,<27",
]
